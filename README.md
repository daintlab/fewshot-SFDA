# source_free_DA

### Requirments (필수)
python == 3.9.12 \
torch == 1.12.1 \
torchvision == 0.13.1 \
numpy == 1.23.5 \
scikit-learn == 1.2.0

python은 conda install로 설치하고 pytorch는 pip install로 설치하면 됨 \
C,D,E 서버에서만 완전 똑같이 재현 가능

### Commands
- Default 학습
